apiVersion: v1
kind: Service
metadata:
  name: {{ include "ks-integration.fullname" . }}
  labels:
    {{- include "ks-integration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: http
  selector:
    {{- include "ks-integration.selectorLabels" . | nindent 4 }}
